<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Notes.Views.AllNotesPage"
             Appearing="ContentPage_Appearing"
             Title="Notes">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Clicked="Add_Clicked" Text="Add" IconImageSource="{FontImage FontFamily=ionicons, Size='80', Glyph='&#xf217;', Color=White}" Order="Primary"/>
    </ContentPage.ToolbarItems>

    <CollectionView x:Name="NotesCollection"
                    Margin="10"
                    ItemsSource="{Binding Notes}"
                    SelectionMode="Single"
                    SelectionChanged="NotesCollection_SelectionChanged">

        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
        </CollectionView.ItemsLayout>
        
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <VerticalStackLayout>
                    <Label Text="{Binding Text}" FontSize="18" />
                    <Label Text="{Binding Date}" FontSize="14" TextColor="DarkBlue" />
                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
        
    </CollectionView>
</ContentPage>
